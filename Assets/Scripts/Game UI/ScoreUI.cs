using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ScoreUI : GenericUI {

	public override void Initialize(Dictionary<Util.PLAYER, Util.COLOR> playerDefs, int foo) {
		base.Initialize (playerDefs);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
